# NOTE: pinned to stay NumPy-1.x compatible. The API venv (spacy/thinc) and
# mediapipe both require numpy<2; opencv-python >=4.11 declares numpy>=2 and will
# silently upgrade numpy, breaking the API. Keep the upper bounds below.
mediapipe>=0.10.9,<0.10.20  # <0.10.20 keeps the legacy mp.solutions.holistic API
opencv-python>=4.9.0,<4.11
numpy>=1.24,<2.0
tqdm>=4.66.0
