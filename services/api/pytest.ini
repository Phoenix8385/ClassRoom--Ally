[pytest]
asyncio_mode = strict
testpaths = tests
# tests/unit has no __init__.py, so pytest puts that directory on sys.path
# rather than the service root — without this, `import app` fails at collection.
pythonpath = .
